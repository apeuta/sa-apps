/**
 * Layout untuk halaman autentikasi (login, callback)
 *
 * Tanpa sidebar dan header — konten ditampilkan centered.
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
